[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: marks tests as unit tests
    integration: marks tests as integration tests
    migration: marks tests related to database schema migrations